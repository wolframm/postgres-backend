const postgres = 'postgres';
const dbPort = 5432;

const appException = 'APPXX';



const int maxServiceSets = 100;

const pb = 'pb';

const pbYaml = '$pb.yaml';

const dbContainerName = 'postgres';

const appSchemaName = 'app';

const milliSecondsPerDay = 24 * 60 * 60 * 1000;

typedef MicroSecond = int;
typedef MilliSecond = int;
typedef Day = int;
